project: empty-test
status: scaffolded
last_session:
  date: 2026-04-30
  summary: >-
    Added GitHub Actions CI (.github/workflows/ci.yml). Triggers on every push
    and on pull requests targeting main. Runs npm ci, npm run lint,
    npx tsc --noEmit, and npm run build on ubuntu-latest with Node 22 and the
    setup-node npm cache. Verified all three commands pass locally before
    pushing. Branch protection on main still needs to be enabled manually in
    GitHub Settings → Branches (instructions in the PR description).
next_focus: Awaiting user direction on the first feature to build.
blockers:
  - Branch protection on main not yet enabled — do this in GitHub Settings → Branches once the CI workflow has run at least once so the `verify` check is selectable.
notes:
  - Supabase, Stripe, and Vercel integrations are not wired yet.
  - Next.js 16.2.4 is newer than baseline training data; consult node_modules/next/dist/docs/ before any Next.js work.
  - .gitignore allows the four .claude/ context files but keeps .claude/settings.local.json ignored.
  - Pre-commit hook lives in .husky/pre-commit and runs `npx lint-staged`. lint-staged config is in package.json.
  - Prettier defaults: semi, double quotes, trailing-comma all, printWidth 100, lf endings (.prettierrc.json).
  - CI workflow is .github/workflows/ci.yml; the job is named `verify` (this is the status name to require in branch protection).
